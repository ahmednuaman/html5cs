var expect = require('expect.js'),
    html5cs = require('../index');

describe('html5cs', function () {
  it('should be happy with good.html', function (done) {
    done();
  });

  it('should complain about bad.html', function (done) {
    done();
  });
});
